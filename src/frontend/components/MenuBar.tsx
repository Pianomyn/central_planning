import * as React from "react";
import { Navbar, Button } from "react-bootstrap";

export default function MenuBar() {
  return (
    <div className="container-fluid">
      <div className="row">
        <Navbar variant="dark">asdf</Navbar>
      </div>
    </div>
  );
}
